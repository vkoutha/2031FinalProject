#include <stdint.h>

#define NUM_DESTINATIONS 12

uint8_t currDestination;
uint64_t destinations[NUM_DESTINATIONS];
uint64_t destinationsOrder[NUM_DESTINATIONS];
uint8_t adjMatrix[NUM_DESTINATIONS][NUM_DESTINATIONS];
uint16_t visitedSet;

int main() {
    
    return 0;
}

void createRoute() {
    int closest = 100000000;
    if()
} 